// src/boot/lucidFlow.ts
import { VueFlow } from '@vue-flow/core';
import { boot } from 'quasar/wrappers';

export default boot(async ({ app }) => {
  //app.use(VueFlow);
  //console.log('VueFlow boot file');
});
